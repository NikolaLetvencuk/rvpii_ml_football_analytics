# ---- Prelimin. analiza: statistike i odnosi [3p] ----------------------------
# Funkcije koje vracaju TABELE (data.table), ne stampaju i ne crtaju.

# deskriptivne_numericke(dt) -> n, NA, min, Q1, median, mean, Q3, max, sd, IQR
# deskriptivne_kategoricke(dt) -> nivo | frekvencija | relativna frekvencija
# korelaciona_matrica(dt, metod = "spearman") -> matrica
# hi_kvadrat_tabela(dt, a, b) -> kontingencija + p-vrednost (kat. vs kat.)
# odnos_sa_ciljem(dt, cilj = CILJ) -> jacina veze svakog obelezja sa ciljem
